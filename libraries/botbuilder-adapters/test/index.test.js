const assert = require('assert');
const { TestAdapter, ConversationState, MemoryStorage } = require('botbuilder');
const MyBot = require("../src/consoleAdapter");

describe('Console adapter tests', () => {

});
